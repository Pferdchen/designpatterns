package visitor.bank;

public class ICBCSheet extends Sheet {

	public ICBCSheet(String account, String name, float money,
			String startDate, int range) {
		super(account, name, money, startDate, range);
	}
}
