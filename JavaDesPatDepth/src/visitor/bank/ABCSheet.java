package visitor.bank;

public class ABCSheet extends Sheet {

	public ABCSheet(String account, String name, float money, String startDate,
			int range) {
		super(account, name, money, startDate, range);
	}
}
